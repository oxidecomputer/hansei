/target

# Tests have a separate build directory.
/test-programs/target

# Rejected goldens a diverged snapshot writes for review.
*.snap.new

# cargo-mutants scratch output.
/mutants.out
/mutants.out.old

# The demo program builds in its own directory.
/example/target
