[package]
name = "test-programs"
version = "0.1.0"
edition = "2024"

[dependencies]
futures = "0.3.32"
oxide-tokio-rt = "0.1.2"
tokio = { version = "1.47.0", features = [ "rt-multi-thread", "sync", "time", "macros" ] }

[profile.release]
debug = true

[[bin]]
name = "futurelock"
